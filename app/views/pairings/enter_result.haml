%h3
  Enter Result For:
  = pairing.to_s
%br

= simple_form_for(pairing, html: {class: 'form-horizontal' }) do |f|
  = f.input :player_1_wins, label: "#{pairing.player_1.to_s} wins"
  = f.input :player_2_wins, label: "#{pairing.player_2.to_s} wins"
  = f.input :draws

  = f.button :submit, class: "btn-primary"
  = link_to "Cancel", :back, class: "btn btn-default"
%br
